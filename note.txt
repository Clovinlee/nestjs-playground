TUTORIAL 
nest new project-name

FOR IMPOR IN DEV DEPENDENCY
ADD --save-dev

# PRSIMA #
npm install prisma --save-dev

for a module to be used in whole project without importing in each class (like prismamodule)
use @global above the @module

form-data vs x-www-form-urlencoded

Barrel Exporn Design Pattern index.js at auth/dto

npm i class-validator class-transformer
npm i argon2 (for verification crypt password etc)
npm i @nestjs/config  <--- for accessing env variables

# JWT #
npm i @nestjs/passport passport 
npm i @nestjs/jwt passport-jwt
npm i -dev @types/passport-jwt
# JWT #

MIXIN is something to combine 2 functionality or code
for example

+ MIXIN +
------------------------------
Class A can swim
Class B can run
Class C = mixin(Class A, Class B)

then Class C can do both swim and run
------------------------------


### USAGE TUTORIAL ###

- user.controller.ts
It has AuthGuard of JWT and @getUser Decorator and it returned a User object

the object originate from auth/strategy/jwt.strategy.ts
the 'jwt' in that strategy is the name of the strategy
